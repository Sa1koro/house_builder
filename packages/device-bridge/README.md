# device-bridge

Phase 2 placeholder for native iOS device features (LiDAR, level, camera calibration).

The web app must treat all measurements as user-provided drafts and preserve source, unit, timestamp, and calibration metadata before using them in estimates.
